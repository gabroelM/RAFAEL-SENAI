const convite = (nomeConvidado,data,horario,local,noivos) => {
alert(`Caro ${nomeConvidado}, convidamos você para comparecer no dia ${data} as ${horario} no/na ${local} para casamento de ${noivos}`)}
let chamada = convite("Luan","02/08","18:00","arena Opus","Gisele e Kauã")